export interface UserInputDTO{
    name: string;
    email: string;
    password: string;
};

export interface UserInputMockDTO{
    id: string,
    name: string;
    email: string;
    password: string;
};