export interface DogWalkingInputDTO {
    status: string,
    date: string,
    price: number,
    latitude: number,
    longitude: number,
    duration: string,
    start_date: string,
    end_date: string
};

export interface DogWalkingInputMockDTO {
    id: string,
    status: string,
    date: string,
    price: number,
    latitude: number,
    longitude: number,
    duration: string,
    start_date: string,
    end_date: string
};